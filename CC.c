#include <stdio.h>
#include <stdlib.h>
#include <assert.h>


int main(){
    FILE *inputP, *outputP;
    int n,m;
    int 

    inputP = fopen("rosalind_CC.txt", "r");
    assert(inputP != NULL);
    
    fscanf(inputP, "%d d", &n, &m);

    






    return 0;
}